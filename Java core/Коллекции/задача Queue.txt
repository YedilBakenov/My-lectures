Напишем задачу на использование LinkedList. Предположим, что у нас есть список задач, которые нужно выполнить.
Мы будем использовать LinkedList для хранения этих задач.


создать класс Task;

Задача:
Cоздать экземпляр класса LinkedList:
 - private LinkedList<String> tasks;

Напишите программу, которая позволяет пользователю добавлять, удалять и просматривать задачи. Каждая задача представляет собой строку текста.

 public TaskList() {
        tasks = new LinkedList<>(); - проришите данный конструктор. Данный код означает, что в конструкторе класса TaskList создается новый объект типа LinkedList с именем tasks. 
Это означает, что tasks будет использоваться для хранения элементов типа Task в виде связанного списка. Конструктор вызывается каждый раз при создании нового экземпляра класса TaskList.
Таким образом, при создании нового объекта TaskList, будет создан новый пустой список tasks, который можно будет использовать для добавления и удаления задач.
    }

прописать методы:
 + public void addTask
 + public void removeTask(int index)
 + public void viewTasks()

в классе Main, запустите цикл на выполнение задач:
"1. Add task";
"2. Remove task";
"3. View tasks";
"4. Exit";

